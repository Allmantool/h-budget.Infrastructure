# Eval Fixtures

Keep small, non-sensitive synthetic inputs here only when a case cannot use repository-native code or tests. Prefer isolated worktrees and existing test fixtures. Do not copy production data, credentials, certificates, database volumes, or build artifacts into this directory.
